# Copyright (c) Abstract Machines
# SPDX-License-Identifier: Apache-2.0

upstream mqtt_cluster {
        server rabbitmq:${SMQ_RABBITMQ_MQTT_PORT};
}

upstream mqtt_tls_cluster {
        server rabbitmq:${SMQ_RABBITMQ_MQTT_TLS_PORT};
}
